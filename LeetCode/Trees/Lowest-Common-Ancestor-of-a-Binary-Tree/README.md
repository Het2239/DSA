<div align="center">

# 🟡 Lowest Common Ancestor of a Binary Tree

![LeetCode](https://img.shields.io/badge/LeetCode-FFA116?style=flat-square) ![Trees](https://img.shields.io/badge/Trees-6c63ff?style=flat-square) ![Medium](https://img.shields.io/badge/Medium-ffc01e?style=flat-square) ![1/**2*definitionforabinarytreenode.3*structtreenode{4*intval;5*treenode*left;6*treenode*right;7*treenode(intx):val(x),left(null),right(null){}8*};9*/10classsolution{11public:12treenode*lowestcommonancestor(treenode*root,treenode*p,treenode*q){13if(!root||root==p||root==q)returnroot;14treenode*left=lowestcommonancestor(root->left,p,q);15treenode*right=lowestcommonancestor(root->right,p,q);16if(left&&right)returnroot;17returnleft?left:right;18}19};](https://img.shields.io/badge/1%2F**2*definitionforabinarytreenode.3*structtreenode%7B4*intval%3B5*treenode*left%3B6*treenode*right%3B7*treenode(intx)%3Aval(x)%2Cleft(null)%2Cright(null)%7B%7D8*%7D%3B9*%2F10classsolution%7B11public%3A12treenode*lowestcommonancestor(treenode*root%2Ctreenode*p%2Ctreenode*q)%7B13if(!root%7C%7Croot%3D%3Dp%7C%7Croot%3D%3Dq)returnroot%3B14treenode*left%3Dlowestcommonancestor(root--%3Eleft%2Cp%2Cq)%3B15treenode*right%3Dlowestcommonancestor(root--%3Eright%2Cp%2Cq)%3B16if(left%26%26right)returnroot%3B17returnleft%3Fleft%3Aright%3B18%7D19%7D%3B-0f9d58?style=flat-square)

[![Problem Link](https://img.shields.io/badge/Open%20Problem-grey?style=flat&logo=link)](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/)

</div>

---

## 📋 Problem Statement

Given a binary tree, find the lowest common ancestor (LCA) of two given nodes in the tree.

According to the definition of LCA on Wikipedia: “The lowest common ancestor is defined between two nodes p and q as the lowest node in T that has both p and q as descendants (where we allow a node to be a descendant of itself).”

 

Example 1:

Input: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1
Output: 3
Explanation: The LCA of nodes 5 and 1 is 3.


Example 2:

Input: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4
Output: 5
Explanation: The LCA of nodes 5 and 4 is 5, since a node can be a descendant of itself according to the LCA definition.


Example 3:

Input: root = [1,2], p = 1, q = 2
Output: 1


 

Constraints:

The number of nodes in the tree is in the range [2, 105].
-109 <= Node.val <= 109
All Node.val are unique.
p != q
p and q will exist in the tree.

**Example 1:**
```
Input: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1
Output: 3
Explanation: The LCA of nodes 5 and 1 is 3.
```

**Example 2:**
```
Input: root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4
Output: 5
Explanation: The LCA of nodes 5 and 4 is 5, since a node can be a descendant of itself according to the LCA definition.
```

**Example 3:**
```
Input: root = [1,2], p = 1, q = 2
Output: 1
```

**Constraints:**

---

## 🤖 AI Topic Classification

| Field | Value |
|-------|-------|
| **Topic** | Trees |
| **Confidence** | High |
| **Reasoning** | The problem involves finding the lowest common ancestor in a binary tree, which is a fundamental operation in tree data structures. |
| **Native Tags** | — |

---

## 💡 Approach — Recursive LCA

> 🧠 *Generated by UniCode AI*

**Key Insight:** This approach works by recursively traversing the binary tree, checking if the current node is one of the target nodes or if the target nodes are in the left or right subtrees, and returning the current node as the LCA if both conditions are met.

### Algorithm Walkthrough

1. Step 1: If the current node is null, return null as there is no LCA in an empty tree.
2. Step 2: If the current node is one of the target nodes (p or q), return the current node as it is the LCA.
3. Step 3: Recursively call the function on the left and right subtrees, and check if both recursive calls return a non-null node. If both return a non-null node, it means the target nodes are in different subtrees, so return the current node as the LCA. Otherwise, return the non-null node returned by one of the recursive calls.
4. Step 4: If only one of the recursive calls returns a non-null node, it means the target nodes are in the same subtree, so return the non-null node.

### Complexity Analysis

| | Complexity | Notes |
|--|------------|-------|
| ⏱️ **Time** | `O(n)` | The time complexity is O(n) because in the worst case, we need to traverse the entire tree. The space complexity is O(h) because the maximum depth of the recursion call stack is the height of the tree, which can be as bad as O(n) in a skewed tree. |
| 🗄️ **Space** | `O(h)` | |

---

## ✅ Accepted Solution 

```1/**2*definitionforabinarytreenode.3*structtreenode{4*intval;5*treenode*left;6*treenode*right;7*treenode(intx):val(x),left(null),right(null){}8*};9*/10classsolution{11public:12treenode*lowestcommonancestor(treenode*root,treenode*p,treenode*q){13if(!root||root==p||root==q)returnroot;14treenode*left=lowestcommonancestor(root->left,p,q);15treenode*right=lowestcommonancestor(root->right,p,q);16if(left&&right)returnroot;17returnleft?left:right;18}19};
/
```

<div align="center">

*Submitted: Jul 16, 2026, 06:25 PM · Platform: LeetCode · Auto-archived by [UniCode](https://github.com/UniCode-ext/unicode)*

</div>
